name: Terraform UG

on:
  push:
    paths:
      - "env/ug/**"
      - ".github/workflows/terraform-ug.yml"

jobs:
  terraform:
    uses: PapsyOps/sandbox-management/.github/workflows/terraform-template.yml@main
    with:
      environment: ug
      working_directory: ./env/ug
      var_file: dev.tfvars
    secrets: inherit
